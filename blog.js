function cliqueArtigo1() {
    var list = document.getElementById("list1");
    var text = document.getElementById("task_name1").value;
  
    if(text.length === 0) {
      alert("Não há nenhum caractere no seu comentário, digite algo.");
      return;
    }
  
    var listItem = document.createElement("li");
    listItem.className = "list-item"
  
    const textElement = document.createTextNode(text);
    listItem.appendChild(textElement)
    list.appendChild(listItem)
  }
  
function cliqueArtigo2() {
    var list = document.getElementById("list2");
    var text = document.getElementById("task_name2").value;

    if(text.length === 0) {
        alert("Não há nenhum caractere no seu comentário, digite algo.");
        return;
    }

    var listItem = document.createElement("li");
    listItem.className = "list-item"

    const textElement = document.createTextNode(text);
    listItem.appendChild(textElement)
    list.appendChild(listItem)
}

function cliqueArtigo3() {
    var list = document.getElementById("list3");
    var text = document.getElementById("task_name3").value;

    if(text.length === 0) {
        alert("Não há nenhum caractere no seu comentário, digite algo.");
        return;
    }

    var listItem = document.createElement("li");
    listItem.className = "list-item"

    const textElement = document.createTextNode(text);
    listItem.appendChild(textElement)
    list.appendChild(listItem)
}

function cliqueArtigo4() {
    var list = document.getElementById("list4");
    var text = document.getElementById("task_name4").value;

    if(text.length === 0) {
        alert("Não há nenhum caractere no seu comentário, digite algo.");
        return;
    }

    var listItem = document.createElement("li");
    listItem.className = "list-item"

    const textElement = document.createTextNode(text);
    listItem.appendChild(textElement)
    list.appendChild(listItem)
}

function cliqueArtigo5() {
    var list = document.getElementById("list5");
    var text = document.getElementById("task_name5").value;

    if(text.length === 0) {
        alert("Não há nenhum caractere no seu comentário, digite algo.");
        return;
    }

    var listItem = document.createElement("li");
    listItem.className = "list-item"

    const textElement = document.createTextNode(text);
    listItem.appendChild(textElement)
    list.appendChild(listItem)
}

function cliqueArtigo6() {
    var list = document.getElementById("list6");
    var text = document.getElementById("task_name6").value;

    if(text.length === 0) {
        alert("Não há nenhum caractere no seu comentário, digite algo.");
        return;
    }

    var listItem = document.createElement("li");
    listItem.className = "list-item"

    const textElement = document.createTextNode(text);
    listItem.appendChild(textElement)
    list.appendChild(listItem)
}

